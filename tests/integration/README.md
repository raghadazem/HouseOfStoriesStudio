# Integration Tests

Reserved for cross-module tests (e.g. asset import writing to both the
filesystem and the database) starting in Milestone 3, once
`app/core/services/` exists. Empty in Milestone 1.
