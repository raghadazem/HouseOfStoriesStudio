# World

Structured mirror of `docs/03_WORLD_BIBLE.md`: one subfolder per
location under `locations/<location_slug>/` holding reference artwork,
plus a `world_bible.yaml` summarizing locations and world rules.

Not yet populated — this folder currently only establishes the layout.
