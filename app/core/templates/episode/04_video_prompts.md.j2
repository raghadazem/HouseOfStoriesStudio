# Video Prompts — {{ episode.title_en }}

Same composition rule as image prompts: always include the relevant
character master prompt block(s) and the shared style-lock block.
Video generation is done manually in external tools (see
`docs/07_DEVELOPMENT_PLAN.md` §11) and imported afterward — nothing
here is called automatically by the application.

## Shot 1

- **Scene reference:**
- **Style lock:**
- **Character lock(s):**
- **Scene-specific prompt:**
- **Negative prompt:**
- **Tool used:**
- **Result asset(s):**
- **Approval status:** draft
