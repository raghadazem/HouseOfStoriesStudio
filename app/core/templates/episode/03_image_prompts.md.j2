# Image Prompts — {{ episode.title_en }}

Prompts are written in English for best results in external
generators, even though the story itself is in Arabic. Every prompt
involving a main character MUST include that character's current
Master Prompt Block (see `production/characters/<name>/character_meta.yaml`)
and the shared style-lock block (`production/prompts/style_lock/`) so
generated art stays on-model.

## Shot 1

- **Scene reference:** (link to storyboard shot #)
- **Style lock:** (paste from `production/prompts/style_lock/visual_style_block.md`)
- **Character lock(s):** (paste relevant character master prompt block(s))
- **Scene-specific prompt:**
- **Negative prompt:** (paste relevant character negative prompt block(s))
- **Tool used:**
- **Result asset(s):**
- **Approval status:** draft
